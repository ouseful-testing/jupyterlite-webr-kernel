name: jupyterlite-webr-kernel
channels:
  - conda-forge
  - conda-forge/label/jupyterlite_core_alpha
dependencies:
  # python
  - python
  - build
  - pip
  - ruff
  # javascript
  - nodejs=20
  # deps
  - jupyterlab >=4.0,<5
  - notebook~=7.1.3
  - jupyterlite-core >=0.3.0rc1

